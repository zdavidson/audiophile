import "./Hero.scss";
import Container from "react-bootstrap/Container";

const Hero = () => {
  return (
    <div id="hero">
      <Container>I need a hero</Container>
    </div>
  );
};

export default Hero;
